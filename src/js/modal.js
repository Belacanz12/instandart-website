import $ from 'jquery';
import 'materialize-css/dist/js/materialize.min';

$(document).ready(function(){
  // let instance = null;
  //
  // $('.modal').each(function(){
  //   instance = M.Modal.init($(this), {});
  // });

  // instance[0].open();
  // console.log(instance)
  // $('.modal').modal();
});
